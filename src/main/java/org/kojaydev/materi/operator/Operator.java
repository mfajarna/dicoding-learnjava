package org.kojaydev.materi.operator;

public class Operator {
    // Operator Assignment ( = )
    int value = 2, anotherValue = 10;
    boolean isTrue = true;

    // Operator Aritmatika ( +, -, *, /, % )
    public int additive = value + anotherValue;
    public int subtraction = value - anotherValue;
    public int multiplication = value * anotherValue;
    public int division = anotherValue / value;
    public int remainder = anotherValue % 2;

    // Operator unary ( + , -, ++, --, !)
    public int positive = +value;
    public int negative = -value;
    public int increment = value++;
    public int decrement = value--;
    public boolean res = !isTrue;

    // Operator Equality and Relational ( ==, !=, > , <, >=, <= )
    // Operator Conditional ( && , || )



}
